

import React from 'react';
import ArrowDropDownIcon from '@mui/icons-material/ArrowDropDown';
import theme from "../../../theme/theme";

const Dropdown = () => {
  return (
    <ArrowDropDownIcon style={{color: theme.palette.grey[200]}} />
  );
};

export default Dropdown;

